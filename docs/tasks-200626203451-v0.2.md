# Tasks — archview v0.2

> Living checklist. Anti-halu tracker. Mulai 2026-06-20 20:34 WIB.
> Sumber: `docs/plan-200626203451-v0.2.md`

Status: `[ ]` todo · `[~]` in-progress · `[x]` done · `[!]` blocked

## DONE this session (post-v0.1)

- [x] **Outbound interface ports** — `analyzer/ports.go` + `Options.ShowPorts`. service→port (uses) + repository→port (implements). Verified hexagonal (3 ports, converging edges, 0 direct leak).
- [x] **Bus detection (mediator/CQRS)** — `analyzer/buses.go` + `Options.DetectBuses`. Pass1 AST learns bus types + message→handler routing from `Register/Subscribe`; bus methods become CHA barriers; Pass2 SSA resolves dispatch sites (unwrap `MakeInterface`). Verified CQRS example: precise 1:1 routing, event sub routing correct, marker noise pruned. Fixed 2 bugs: value-receiver wrapper in `methodFunc`, interface boxing.
- [x] **gRPC route extractor** — `route/grpc.go` (structural: `Register*Server` + service interface methods). In `Default()`. Verified grpc example: 3 RPC endpoints → server methods → service → repo.
- [x] **classify module-prefix fix** — strip `res.Module` before classify so module name (e.g. ending in "grpc") can't false-match a layer keyword. `build.classify()`.
- [x] Examples added: `examples/cqrs` (:8095), `examples/grpc` (:8096).

## A. Analysis depth

### A1 — Outbound / external detection
- [ ] A1.1 Define `graph.KindExternal` node + `graph.EdgeOutbound`
- [ ] A1.2 Detector interface `OutboundDetector` (match callee pkg/func → external label)
- [ ] A1.3 Impl: database/sql, pgx, gorm (DB), net/http client + resty (HTTP), redis, amqp/kafka, gRPC client
- [ ] A1.4 Wire di build: edge repo/service → external node
- [ ] A1.5 Render external node (distinct style, kolom paling kanan)
- [ ] A1.6 Example: repo yang beneran `sql.Open`/`http.Get` → verify node muncul

### A2 — Layer-violation detector
- [ ] A2.1 Rule set: controller→repo (skip), repo→service/controller (reverse), cross-module
- [ ] A2.2 `Edge.Violation string` + compute di build
- [ ] A2.3 Render edge merah + panel "violations" di web
- [ ] A2.4 Example sintetis: controller manggil repo langsung → ke-flag

### A3 — Join prefix path
- [ ] A3.1 gin: track `RouterGroup.Group(prefix)` per receiver var → join
- [ ] A3.2 net/http: handle `http.StripPrefix` + sub-mux mount prefix
- [ ] A3.3 Verify gin-mvc nampilin `/api/users` (bukan `/users`)

## A4 — Skip trivial helper nodes (noise reduction)
- [ ] A4.1 Heuristik deteksi "trivial helper": free function (recv == nil) di layer terklasifikasi, kecuali route/RPC handler. Kandidat sinyal: no receiver, body kecil/leaf, lowercase (unexported), cuma return value / format string.
- [ ] A4.2 Opsi: exclude dari node, ATAU collapse-through (kayak helper non-layer sekarang) biar edge caller→callee tetep nyambung.
- [ ] A4.3 Hati-hati false-positive: sebagian project pakai free-function handler (net/http HandlerFunc) atau free-function service. Jangan buang yang jadi route handler (udah force-included) atau yang punya downstream ke layer lain.
- [ ] A4.4 Verify: `id`-style helper (lihat grpc example, sempat muncul sebelum di-inline) ga lagi jadi node di codebase real tanpa ubah source.
> Asal-usul: 2026-06-21, grpc example `func id(n int) string` muncul sebagai node service + edge intra-column jelek. Di-workaround dgn inline di example; fix asli = heuristik ini.

## B. Web UX
- [ ] B1 Search box (filter node by label) + toggle layer/module visibility
- [ ] B2 Klik endpoint → highlight full downstream path (BFS)
- [ ] B3 Persist layout (laneOffset + nodeDelta + collapsed) ke localStorage, keyed by module
- [ ] B4 Live reload: on-request re-analyze opt-in + SSE push (fsnotify)

## C. Breadth
- [ ] C1 echo route adapter
- [ ] C2 fiber route adapter
- [ ] C3 chi route adapter
- [ ] C4 Prod-baked: `cmd/archview` gen → graph.json + go:embed loader
- [ ] C5 Export PNG / SVG / mermaid / dot

## D. Web docs (fumadocs) — DONE (kecuali deploy)
- [x] D1 Scaffold fumadocs di `website/` (bun, Next 16 + fumadocs 16, src dir)
- [x] D2 Config: shared.ts (appName=archview, git=eularixs/archview), home landing, meta.json nav
- [x] D3 Content MDX (12 page): Introduction, Quickstart, Installation, Configuration, How it works, Layer classification, Patterns, Frameworks, UI guide, Examples, Limitations, Roadmap
- [x] D4 `bun run build` lulus — 12 doc route + home + search + OG generate, serve 200
- [ ] D5 Link dari README + deploy Vercel (eularixs) — nunggu greenlight commit/push

## FIX di luar plan (ketemu sambil jalan)
- [x] Module path bug: `go.mod` & semua import `github.com/eularix/archview` (singular) → `github.com/eularixs/archview` (match repo). 9 go file + README + 4 docs. Build OK.
  - [!] v0.0.1-beta yang ke-publish masih path lama → `go get @v0.0.1-beta` BROKEN. Perlu re-tag (v0.0.2-beta) atau replace tag. Nunggu greenlight.

## Known constraint
- Outbound detection = heuristik per-driver; ga semua lib ke-cover. Log yang ke-skip.
- Multi-impl interface masih over-approx (CHA) — relevan buat violation false-positive; catat.
