# Tasks — archview v0.2 (post-v0.1.0)

> Dibuat: 2026-06-22 07:08 WIB. Konsolidasi pasca-rilis v0.1.0.
> Status v0.1.0: SHIPPED (`eularixs/archview@v0.1.0`) — net/http+gin+gRPC, MVC+hexagonal(ShowPorts)+CQRS(DetectBuses), web redesign, docs site.

Status: `[ ]` todo · `[~]` in-progress · `[x]` done · `[!]` blocked

## Prinsip
- Tiap fitur framework = 1 `Extractor` impl, masuk `route.Default()`, opt-out lewat `Options.Extractors`.
- Tiap fitur analisa berat = opt-in `Options.*` (default off), default graph ga berubah.
- Tiap fitur WAJIB punya example + verify via `/graph/data` (proof, bukan klaim).

---

## P1 — Reach: protokol baru

### T1. GraphQL extractor (gqlgen) — DONE
- [x] T1.1 `route/graphql.go` — `graphqlExtractor`. Match struktural: interface `Query/Mutation/SubscriptionResolver` (scope sendiri + import) di-implement project type.
- [x] T1.2 Tiap method interface → endpoint. Method = QUERY/MUTATION/SUBSCRIPTION, path = `/<Type>/<field>` (lcFirst).
- [x] T1.3 Handler = method konkret (`methodByName`, reuse grpc helper). In `Default()`.
- [~] T1.4 Nested field resolver (per-type, mis. `orderResolver`) — SKIP MVP, lihat limitation.
- [x] T1.5 Example `examples/graphql/` (:8097). Verified: 4 field (Query.order/orders, Mutation.createOrder/cancelOrder) → resolver → service → port → repo. 17 node bersih, gin/dll no regression.
- Limitation: cuma root Query/Mutation/Subscription resolver; nested field resolver belum. Subscription terdeteksi kalau interface-nya ada.

### T2. WebSocket
- [ ] T2.1 Konfirmasi upgrade endpoint kebaca (gorilla `upgrader.Upgrade`, `nhooyr/coder` Accept) — harusnya udah via net-http/gin route. Verify + kasih label "WS".
- [ ] T2.2 Per-message routing — registry-based: extend mesin `analyzer/buses.go` biar deteksi `register(msgType, handler)` di WS dispatcher (pola sama bus). Edge conn-loop → message handler.
- [ ] T2.3 Per-message routing — switch-based (`switch msg.Type { case X: handleX() }`): heuristik AST baca case→handler call. Lebih susah; fase 2 / catat sebagai limitation kalau ga sempat.
- [ ] T2.4 Example `examples/websocket/` (registry-based dulu). Verify entry + per-message edge.

### T3. echo / fiber / chi adapter
- [ ] T3.1 `route/echo.go` — `e.GET/POST/...` di `*echo.Echo`/`*echo.Group`.
- [ ] T3.2 `route/fiber.go` — `app.Get/Post/...` di `*fiber.App`/`*fiber.Router`.
- [ ] T3.3 `route/chi.go` — `r.Get/Post/...` + `r.Route`/`r.Mount` (prefix).
- [ ] T3.4 Add ke `Default()`. Example masing-masing atau 1 multi-framework example.

---

## P2 — Guardrail & akurasi

### T4. Layer-violation linter
- [ ] T4.1 Rule set: controller→repository (skip service), repository→service/controller (reverse dep), cross-module call bocor, service→controller.
- [ ] T4.2 `Edge.Violation string` (kind/severity) — compute di `build` dari layer+module tiap edge.
- [ ] T4.3 Render: edge merah/warning + panel "violations" di web (list, klik→highlight).
- [ ] T4.4 `Options.LintLayers bool` opt-in. Example sintetis (controller manggil repo langsung) → ke-flag.

### T5. Trivial-helper filter (carry-over A4) — DONE
- [x] T5.1 `trivialHelper(f)` = free function (recv=="") + unexported. Route/RPC/resolver handler aman (method / force-included).
- [x] T5.2 Collapse-through (skip dari `included` → CHA collapse lewat helper, edge caller→downstream tetep nyambung).
- [x] T5.3 Guard: exported free-func (mis. `New`) ga ke-hide; net/http free-func handler force-included.
- [x] T5.4 `Options.ShowHelpers bool` — default false (hidden). Verified: `normalizeID` temp helper ABSENT default. `build.Graph` refactor ke `build.Options`.

### T6. Join route prefix
- [x] T6.1 gin: `ginGroupPrefixes` track `RouterGroup.Group(prefix)` per var (transitive resolve) → `joinPath`. Verified gin-mvc nampilin `/api/users`, `/api/products`, `/api/users/:id`.
- [ ] T6.2 net/http: `http.StripPrefix` + sub-mux mount prefix. (deferred)
- [ ] T6.3 chi: `r.Route("/api", ...)` nesting. (deferred — chi extractor sendiri T3.3)
- Limitation: chained `r.Group("/api").GET(...)` tanpa var intermediate belum (cuma var-assigned group).

### T7. Outbound external detection (DB/HTTP/queue)
- [ ] T7.1 `graph.KindExternal` + `EdgeOutbound`. Node external di kolom paling kanan.
- [ ] T7.2 `OutboundDetector` — match callee pkg/func → external label: `database/sql`/`pgx`/`gorm` (DB), `net/http` client/`resty` (HTTP), `redis`, `kafka`/`amqp`, gRPC client.
- [ ] T7.3 Wire: edge repository/service → external node.
- [ ] T7.4 `Options.DetectOutbound bool`. Example repo yang `sql.Open`/`http.Get` → node muncul.

---

## P3 — UX & distribusi

### T8. Web UX
- [ ] T8.1 Search box (filter node by label) + toggle layer/module visibility.
- [ ] T8.2 Klik endpoint → highlight full downstream path (BFS).
- [ ] T8.3 Persist layout (laneOffset+nodeDelta+collapsed) → localStorage, keyed by module.
- [ ] T8.4 Live reload: on-request re-analyze opt-in + SSE push (fsnotify).

### T9. Prod-baked mode
- [ ] T9.1 `cmd/archview gen` → analyze sekali → emit `graph.json`.
- [ ] T9.2 `archview.FromJSON(embed)` loader → serve tanpa source/toolchain runtime.
- [ ] T9.3 Example deployed-binary.

### T10. Export
- [ ] T10.1 Export graph → mermaid / dot / SVG / PNG (tombol di toolbar + endpoint `/graph/export?format=`).

### T11. Docs site deploy
- [ ] T11.1 Deploy `website/` ke Vercel (eularixs, identity Eularix). Domain nyusul.
- [ ] T11.2 Link dari README + repo About.
- [ ] T11.3 Tambah docs page: gRPC sudah; tambah GraphQL/WebSocket pas T1/T2 kelar.

---

## Urutan saran
1. **T1 GraphQL** (gampang-medium, lengkapin protokol, pola grpc udah ada).
2. **T5 trivial-helper** + **T6 prefix** (polish murah, naikin kualitas graph).
3. **T4 layer-violation** (killer feature, guardrail).
4. **T2 WebSocket** (entry + registry routing).
5. **T7 outbound external** (lengkapin visi awal "outbound kebaca").
6. **T8 UX** + **T11 deploy docs**.
7. **T3 echo/fiber/chi**, **T9 prod-baked**, **T10 export** (breadth).

## Catatan akurasi (jujur, bawa ke limitation)
- WebSocket switch-based routing & multi-impl interface non-bus = tetep over-approx/invisible (Tier C).
- GraphQL nested field resolver belum (MVP cuma root Query/Mutation).
