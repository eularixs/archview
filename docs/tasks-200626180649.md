# Tasks — archview v0.1

> Living checklist. Anti-halu tracker.
> Module: `github.com/eularix/archview` · Status: **v0.1 MVP DONE**

Status: `[x]` done · `[!]` partial

## Core
- [x] Setup go module + x/tools v0.46.0
- [x] graph/ — Node/Edge/Graph + EditorURL (vscode/cursor)
- [x] classify/ — layer heuristik (MVC + hexagonal vocab) + module derive + Config
- [x] analyzer/ — go/packages + SSA + CHA callgraph + FuncFor bridge
- [x] route/ — Extractor interface + gin + net/http (method-prefix parse) + auto-detect
- [x] build/ — endpoint/route + collapse helper + prune isolated + deterministic sort
- [x] web/ — /graph + /data, SVG renderer, swimlane, drag node + drag lane, collapse, pan/zoom, toolbar, click→editor
- [x] archview.go — Options/New/Handler/Mount

## Examples + proof
- [x] examples/gin-mvc (modular MVC, gin) — /graph/data verified: 5 endpoint + 15 func, 15 edge
- [x] examples/hexagonal (ports & adapters, net/http) — verified: rest→usecase→postgres+gateway, outbound fan-out, 2 context
- [x] README + docs (plan, prd, claude, context)
- [!] PROOF visual browser — chrome extension off, ga ke-eyeball. JS syntax OK + served OK.

## Known limitation v0.1
- CHA: single-impl interface akurat; multi-impl over-approx.
- Path literal (`/users`), prefix group (`/api`) belum di-join.
- net/http + gin. Dev-live only.
- Inline closure handler → endpoint tanpa link.

## Next (opini, fase 2)
- Outbound/external detection (DB/HTTP/queue) — lengkapin visi.
- Layer-violation linter (controller→repo skip, arah kebalik).
- Join prefix path.
- search/filter, persist layout, live reload.
- echo/fiber/chi, prod-baked, export.
